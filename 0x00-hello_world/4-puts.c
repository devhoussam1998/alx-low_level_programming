/*
 * This program outputs a quote about programming to the console.
 * The quote says "Programming is like building a multilingual puzzle".
 * 
 * The main function uses the puts function to output the quote to the console.
 * The puts function automatically adds a newline character at the end of the string,
 * so there's no need to manually add one.
 * 
 * Finally, the main function returns 0, which is a conventional value to indicate
 * that the program has executed successfully.
 */
#include <stdio.h>

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	/* Return 0 to indicate successful execution */
	return (0);
}
